parameters:
    git_dir: .
    bin_dir: vendor/bin
    tasks:
        composer:
            file: ./composer.json
            no_check_all: true
        git_conflict: ~
        securitychecker: ~
        phpcs:
            standard: "phpcs.xml"
            ignore_patterns: ['ansible', 'bin', 'config', 'docs', 'public', '*/DataFixtures', '*/Migrations', 'templates', 'tests', 'var', 'vendor']
        phplint:
            exclude: ['app', 'bin', 'build', 'tests', 'var', 'vendor', 'web']
        phpmd:
            ruleset: ['codesize.xml']
            exclude: ['ansible', 'bin', 'config', 'docs', 'public', '*/DataFixtures', '*/Migrations', 'templates', 'tests', 'var', 'vendor']
        phpparser:
            kind: php7
            ignore_patterns: ['app', 'bin', 'build', 'tests', 'var', 'vendor', 'web']
            visitors:
                forbidden_function_calls:
                    blacklist:
                        - 'var_dump'
                        - 'exit'
        phpversion:
            project: '7.0'
