List of new active directory users
----------------------------------

Hi ICT Team!

a series of accounts [{{ users|length }}] have just been created for your new users.
After verification, all that remains is to assign the O365 licenses.

{% for user in users %}
    -----------------------------------------------------------------------------------------------------------------------------
    | #:                    {{ user.EmployeeId|default('unknow') }}
    | Firstname:            {{ user.FirstName|default('unknow') }}
    | Lastname:             {{ user.LastName|default('unknow') }}
    | Email:                {{ user.Email }}
    | Country workplace:    {{ user.Department|default('unknow') }} ({{ user.Country|default('unknow') }})
    | Organizational Unit:  {{ user.OrganizationalUnit|default('unknow') }}
    | Generated password:   {{ user.password|default('not generated')|raw }}
    | Start date:           {{ user.StartDate|default('unknow') }}
    | End date:             {{ user.EndDate|default('unknow') }}
    | Log:                  {{ user.log|raw }}
    -----------------------------------------------------------------------------------------------------------------------------

{% endfor %}
