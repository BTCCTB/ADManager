List of users with Organizational Unit changes
----------------------------------------------

Hi ICT Team!

a series of accounts [{{ users|length }}] have just been moved in your Active Directory.
Check the list below:

{% for user in users %}
    -----------------------------------------------------------------------------------------------------------------------------
    | #:                    {{ user.EmployeeId|default('unknow') }}
    | Firstname:            {{ user.FirstName|default('unknow') }}
    | Lastname:             {{ user.LastName|default('unknow') }}
    | Email:                {{ user.Email }}
    | Country workplace:    {{ user.Country|default('unknow') }}
    | Organizational Unit:  {{ user.OrganizationalUnit|default('unknow') }}
    | From:                 {{ user.from|default('unknow') }}
    | To:                   {{ user.to|default('unknow') }}
    | Log:                  {{ user.log }}
    -----------------------------------------------------------------------------------------------------------------------------

{% endfor %}
