List of users updated
---------------------

Hi ICT Team!

a series of accounts have just been updated in your Active Directory.
Check the list below.

{% for user in users %}
    -----------------------------------------------------------------------------------------------------------------------------
    | Firstname:            {{ user.FirstName|default('unknow') }}
    | Lastname:             {{ user.LastName|default('unknow') }}
    | Email:                {{ user.Email|default('unknow') }}
    | Country workplace:    {{ user.Department|default('unknow') }} [{{ user.Country|default('unknow') }}]
    | Log:                  {{ user.log }}
    | Diff:
    {% for val in user.diff %}
        |       - {{ val.attribute }}: {{ val.original|json_encode }} -> {{ val.value|json_encode }}
    {% endfor %}
    |
    -----------------------------------------------------------------------------------------------------------------------------
{% endfor %}
