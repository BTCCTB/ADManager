{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="{{ button_class|default('btn btn-outline-primary') }}"><i class="{{ button_icon|default('fa fa-save') }}" aria-hidden="true"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
