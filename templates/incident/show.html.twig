{% extends 'base.html.twig' %}

{% block title %}Incident{% endblock %}

{% block body %}

    <h1>Incident</h1>

    <table>
        <tr>
            <th>Id</th>
            <td>{{ incident.id }}</td>
        </tr>

    </table>

    <a href="{{ path('incident_index') }}">back to list</a>

    <a href="{{ path('incident_edit', {'id': incident.id}) }}">edit</a>

    {% include 'incident/_delete_form.html.twig' with {'identifier': incident.id} only %}

{% endblock %}