<header>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-secondary">
        <a class="navbar-brand" href="{{ path('homepage') }}">
            <img alt="Enabel - Password manager" height="100%" class="rounded" src="{{ asset('img/logothumb.png') }}">
            Password Manager
        </a>
        <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#navbarAdmin" aria-controls="navbarMenu" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarMenu">
            <ul class="navbar-nav mr-auto">
                {% include ':partial:menu.html.twig' %}
            </ul>
            <ul class="navbar-nav">
            {%  if is_granted('ROLE_USER') %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"><i class="fa fa-user"></i> {{ app.user.username }} <span class="caret"></span></a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                        <a href="{{ path('my_account') }}" class="dropdown-item"><i class="fa fa-user"></i> My account</a>
                        <a href="{{ path('security_logout') }}" class="dropdown-item"><i class="fa fa-unlock"></i> Logout</a>
                    </div>
                </li>
            {%  else %}
                <li class="nav-item"><a href="{{ path('security_login') }}" class="nav-link"><i class="fa fa-lock"></i></a></li>
            {% endif %}
            </ul>
        </div>
    </nav>
</header>
